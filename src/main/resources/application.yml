spring:
  cloud:
    gateway:
      routes:
        - id: mds-route
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/mds/**

#security:
#  authentication:
#    excluded-paths:
#      - /login
#      - /register
